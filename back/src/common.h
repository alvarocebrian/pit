extern char PIT_PATH[50];

#define PIT_DIR ".pit"

#define ARRAY_SIZE(x) (sizeof(x) / sizeof((x)[0]))

/*typedef struct str_array {
    // array length
    int length;

    // array value
    void *value ;
} array;
*/